const stack = [];

stack.push('Snow');
stack.push('Dayneris');
stack.push('Drogo');
stack.push('Sansa');
stack.push('Aria');

console.log(stack);

// .pop() and .push() are using for stack

stack.pop();
console.log(stack);

stack.pop();
console.log(stack);

stack.pop();
console.log(stack);

stack.pop();
console.log(stack);

stack.pop();
console.log(stack);
